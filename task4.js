// Task-4:
// If a given string has either x, replace x by y. if the given string has X, replace it by Y.


let text = "bAngladesh".toLowerCase();

let replaces = text.replaceAll('a',"y")
console.log(replaces)